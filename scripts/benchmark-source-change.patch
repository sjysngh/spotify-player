--- a/spotify_player/src/utils.rs
+++ b/spotify_player/src/utils.rs
@@ -2,7 +2,7 @@
 
 /// formats a time duration into a "{minutes}:{seconds}" format
 pub fn format_duration(duration: &chrono::Duration) -> String {
-    let secs = duration.num_seconds();
+    let secs = duration.num_seconds().max(0);
     format!("{}:{:02}", secs / 60, secs % 60)
 }
 
@@ -95,3 +95,15 @@
         })
         .collect::<Vec<_>>()
 }
+
+#[cfg(test)]
+mod benchmark_duration_tests {
+    use super::format_duration;
+
+    #[test]
+    fn negative_duration_is_clamped_to_zero() {
+        assert_eq!(format_duration(&chrono::Duration::seconds(-61)), "0:00");
+        assert_eq!(format_duration(&chrono::Duration::seconds(0)), "0:00");
+        assert_eq!(format_duration(&chrono::Duration::seconds(65)), "1:05");
+    }
+}
